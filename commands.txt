git config --list
git config --list --show-origin
git config --global user.name "Irwin Estrada"
git config --global user.email "irwinestradatorres@gmail.com"

Staging
git init
git status
git rm --cached commands.txt (trackear / untracked)
git rm --force (Elimina los archivos de Git y del disco duro)

Repository Local
git commit -m "Add commands - urls"
git commit -am "Message"

Repository Remote
git fetch
git merge
git pull origin master --allow-unrelated-histories
git remote add origin https://github.com/irwinet/app_git_proyecto1.git
git remote -v
git remote set-url origin git@github.com:irwinet/app_git_proyecto1.git
git push origin master

Ramas
git branch cabecera
git checkout cabecera

SSH Key
ssh-keygen -t rsa -b 4096 -C "irwinestradatorres@gmail.com"
eval $(ssh-agent -s)
ssh-add ~/.ssh/id_rsa

Otros
git log commands.txt
git diff eafcf99e694de4a5e1f488f5f2ced361421ccfd6 199334511685521f954ba14829dcdf7cec37041e
git reset 2464ce2ea552e8f3524dd9ad69dce77f07310af8 (--hard --soft HEAD) (Este comando nos ayuda a volver en el tiempo)

git checkout 2464ce2ea552e8f3524dd9ad69dce77f07310af8 commands.txt
git checkout master commands.txt

git log --all --graph --decorate --oneline
alias arbolito="git log --all --graph --decorate --oneline"

Tags:
git tag -a v0.1 -m "Advanced Commands" e1602a2
git tag
git show-ref --tags
git push origin --tags
git tag -d NAME_TAG
git push origin :refs/tags/NAME_TAG